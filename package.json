{
  "name": "@cloudflare/agents-mcp-client-example",
  "description": "MCP client demo — connect to remote MCP servers with OAuth support",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite dev",
    "deploy": "vite build && wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false"
  },
  "dependencies": {
    "@cloudflare/agents-ui": "*",
    "@cloudflare/kumo": "^1.7.0",
    "@phosphor-icons/react": "^2.1.10",
    "agents": "*",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4",
    "tailwindcss": "^4.2.0"
  }
}
